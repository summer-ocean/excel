VBA > テーブル > 行
# セル
## セル範囲
```vba
set row = テーブル.ListRows(i)
set cell = row.Range
```

## 各セル
インデックスは左の列から1～  
```vba
set row = テーブル.ListRows(i)
set cell1 = row.Range(1)
set cell2 = row.Range(2)
```
